<Window x:Class="FillWords.WPF.WGame"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FillWords.WPF"
        mc:Ignorable="d"
        Title="WGame" Height="450" Width="800" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" x:Name="RowField"></RowDefinition>
            <RowDefinition Height="150"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Canvas Grid.ColumnSpan="3" Grid.Row="0" x:Name="Field">
        </Canvas>
        <Button FontSize="20" Grid.Row="1" IsCancel="True" Click="BtnCancel_Click">Cancel</Button>
        <TextBlock Grid.Column="1" x:Name="tbWords" Grid.Row="1"></TextBlock>
        <Button Grid.Column="2" Grid.Row="1" FontSize="20" Click="BtnSaveAndExit_Click">Сохранить и выйти</Button>
    </Grid>
</Window>